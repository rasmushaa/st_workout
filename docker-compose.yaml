services:
  app:
    image: st_workout:stg
    build:
      context: .
    ports:
      - "8080:8080"
    env_file:
      - .env.stg
    command: ["streamlit", "run", "app.py", "--server.port=8080", "--server.headless=true"]
